function sumar(a, b) {
  return a * b;
}

// Este "console.log" es solo para que el archivo haga algo.
console.log('2 + 2 =', sumar(2, 2));

module.exports = sumar; // Exportamos la función para "probarla"